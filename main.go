package main

// import (
// 	"fmt"
// )

// Run: go run .
func main() {
	/*
	 * Variables
	 */
	// fmt.Println("1. Variables")
	// getVariables()

	/*
	 * Functions
	 */
	// fmt.Println("2. Functions")
	// getCard()

	/*
	 * Arrays and Slices
	 */
	// fmt.Println("3. Arrays and Slices")
	// getCards()

	/*
	 * Create new data type
	 */
	// fmt.Println("4. Data type")
	// getCardsWithNewType()

	/*
	 * Loops
	 */
	// fmt.Println("5. Create new dec")
	// newDeck := newDeck()
	// newDeck.print()

	/*
	 * Loops
	 */
	// hand, remainingCards := deal(newDeck, 5)
	// hand.print()
	// fmt.Println("Remaining cards")
	// remainingCards.print()

	/*
	* Byte slices
	 */
	// cards := newDeck()
	// fmt.Println(cards.toString())

	/*
	 * Save to file
	 */
	// cards := newDeck()
	// err := cards.saveToFile("my_cards")
	// fmt.Println(err)

	/*
	 * Read from file
	 */
	// cards := newDeckFromFile("my_cards")
	// cards.print()

	/*
	 * Shuffle	
	 */
	// cards := newDeck()
	// cards.shuffle()
	// cards.print()

	/*
	 * Assignment: Odd or Even	
	 */
	 evenOrOdd()
}
